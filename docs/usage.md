# Usage

## Writing pages
- Create a new Markdown file in `docs/`.
- Add it to the navigation in `mkdocs.yml` under `nav:`.

## Linking
- Use relative links: `[Setup](setup.md)`

## Images
- Put images in `docs/img/` and reference them:
  ```
  ![Diagram](img/diagram.png)
  ```